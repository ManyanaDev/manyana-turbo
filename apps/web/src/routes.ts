export const publicRoutes = ["/components", "/blog/:slug"];

export const authRoutes = ["/login", "/register", "/register/:slug", "/error"];

export const apiAuthPrefix = "/api/";

export const DEFAULT_LOGIN_REDIRECT = "/account";
